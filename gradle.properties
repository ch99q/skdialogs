# The plugin compiles directly against Paper's dialog API, so this is the exact Paper build it
# targets: a breaking API change fails the build here. Bump it to the server build you run (this is
# paper-26.2 build 60), and fix any compile errors the change surfaces.
paperVersion=26.2.build.60-beta
skriptVersion=2.12.0

group=com.ch99q
version=1.0.0
