# The exact Paper build the addon compiles against: a breaking dialog API change fails the build
# here. Bump it to the build you run, and fix what no longer compiles.
paperVersion=26.2.build.60-beta
skriptVersion=2.12.0

group=com.ch99q
version=1.1.0
